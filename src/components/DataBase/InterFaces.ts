export interface suraData {
    sura: (string | number)[]
    index : number
}

export interface HeaderName {
    headerName: string;
    headerSubName?: string;
    isSetting: boolean;
}
export interface QuranAyehs {
    ayeh : (string | number)[] ;
}